<main class="container mt-5">
    <div class="row">
        <div class="col-12">
            <h1 class="mb-4">Dashboard</h1>
            <a href="/new-post" class="btn btn-primary mb-4">New Post</a>
        </div>
    </div>
    <br>
    <h1 class="mb-4">Car Blog Post:</h1>
    {{#each posts}}
    <div class="post-container post mb-3">
        <h2><a href="/post/{{id}}" class="text-decoration-none text-reset">{{title}}</a></h2>
        <p>Created by: {{user.username}} | Date: {{format_date crestedAt}}</p>
        <a href="/edit-post/{{id}}" class="btn btn-warning">Edit Post</a>
        <button class="btn btn-danger delete-post" data-id="{{id}}">Delete Post</button>
    </div>
    {{/each}}
</main>